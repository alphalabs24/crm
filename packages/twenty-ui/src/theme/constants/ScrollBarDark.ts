import { GRAY_SCALE } from './GrayScale';
import { RGBA } from './Rgba';

export const SCROLL_BAR_DARK = {
  thumb: {
    background: RGBA(GRAY_SCALE.gray0, 0.2),
    hover: RGBA(GRAY_SCALE.gray0, 0.3),
  },
  track: {
    background: RGBA(GRAY_SCALE.gray0, 0.05),
  },
};
