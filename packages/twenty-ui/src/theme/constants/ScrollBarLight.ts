import { GRAY_SCALE } from './GrayScale';
import { RGBA } from './Rgba';

export const SCROLL_BAR_LIGHT = {
  thumb: {
    background: RGBA(GRAY_SCALE.gray100, 0.2),
    hover: RGBA(GRAY_SCALE.gray100, 0.3),
  },
  track: {
    background: RGBA(GRAY_SCALE.gray100, 0.05),
  },
};
